import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Auto-Recognition System',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
