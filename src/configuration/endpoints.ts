import { baseUrl } from "./api"

export class Endpoints {
    
    readonly licensePlate = `licenseplates/history`
    readonly registeredlicensePlate = `licenseplates/register/`;
    readonly person = `person/history/`;
    readonly registeredperson = `person/register/`;

}
